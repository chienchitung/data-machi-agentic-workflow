---
title: "Day 28｜從會議錄音到行動項目：打造閉環知識工作流"
sidebarTitle: "Day 28｜會議工作流"
description: "完整拆解企業會議錄音轉可追蹤行動項目的 AI 知識工作流：從 ffmpeg 音訊正規化、長音檔靜音點切割、Groq Whisper 語音轉錄，到以固定 JSON Schema 結構化輸出摘要與 Action Items，再自動匯入任務看板並保留來源會議連結。"
---

企業中大量的決策與知識在會議中產生，卻很少能有效地流入可搜尋、可追蹤的工作系統。一段錄音只是保存了聲音；真正有價值的，是把討論內容轉化成摘要、決議、行動項目，並讓這些資訊能夠被後續跟進。這正是 AI 工作流能發揮最大槓桿效益的場景之一。

## 整條工作流的全貌

```text
錄音（瀏覽器 / 上傳）
  → 音訊正規化（ffmpeg）
    → 長音檔分段處理
      → Whisper 語音轉錄（Groq API）
        → 結構化會議記錄（Gemini / GPT-4o）
          → Action Items 提取
            → 匯入任務看板
              → 後續追蹤與會議上下文重用
```

## 逐步拆解每個環節

<Steps>
  <Step title="音訊正規化：讓 Whisper 更容易轉錄">
    瀏覽器錄製的音訊格式多樣（WebM、OGG、MP4），取樣率和聲道數也不一致。在送入 Whisper 前，先用 **ffmpeg** 統一處理，能顯著提升轉錄準確率。

    ```bash
    ffmpeg -i input_audio.webm \
      -ar 16000 \
      -ac 1 \
      -c:a pcm_s16le \
      output_audio.wav
    ```
  </Step>
  <Step title="長音檔分段：處理上傳限制">
    Whisper API 有單次上傳的檔案大小限制。超過 30 分鐘的會議錄音通常需要切割。

    分段策略需注意：

    - 切割點要避開句子中間，盡量在靜音處切割
    - 保留前後重疊片段，避免跨段語意被截斷
    - 平行處理各分段，縮短整體等待時間
  </Step>
  <Step title="Whisper 語音轉錄">
    Data Machi 使用 Groq 提供的 Whisper 端點，並透過分段平行處理長音檔。
  </Step>
  <Step title="結構化會議記錄：固定 Schema">
    逐字稿交給 LLM 後，要求輸出固定 JSON Schema，而不是自由格式的「幫我整理會議」。這是後續自動化能穩定運作的關鍵。
  </Step>
  <Step title="Action Items 匯入看板與後續追蹤">
    將結構化的行動項目匯入任務系統，並保留來源會議 ID，讓每個待辦都能追溯到原始討論。
  </Step>
</Steps>

## 音訊分段範例

```python
from pydub import AudioSegment
from pydub.silence import detect_nonsilent

def split_audio_by_silence(audio_path: str, max_chunk_ms: int = 600_000):
    audio = AudioSegment.from_file(audio_path)
    chunks = []
    start = 0

    while start < len(audio):
        end = min(start + max_chunk_ms, len(audio))
        search_region = audio[max(start, end - 30_000):end]
        silence_ranges = detect_nonsilent(
            search_region,
            min_silence_len=500,
            silence_thresh=-40
        )

        if silence_ranges:
            cut_point = start + (end - 30_000) + silence_ranges[-1][0]
        else:
            cut_point = end

        chunks.append(audio[start:cut_point])
        start = cut_point

    return chunks
```

## Whisper 語音轉錄

```python
from groq import Groq

client = Groq()

async def transcribe_audio_chunk(audio_path: str, language: str = "zh") -> str:
    with open(audio_path, "rb") as audio_file:
        transcription = client.audio.transcriptions.create(
            file=(audio_path, audio_file.read()),
            model="whisper-large-v3",
            language=language,
            response_format="verbose_json",
            timestamp_granularities=["segment"]
        )
    return transcription.text

async def transcribe_full_meeting(chunks: list) -> str:
    tasks = [transcribe_audio_chunk(chunk) for chunk in chunks]
    results = await asyncio.gather(*tasks)
    return "\n".join(results)
```

<Note>
  如果會議涉及專業術語或中英混用，可以在 Prompt 參數提供術語提示，提升特定詞彙的辨識準確率。
</Note>

## 結構化會議記錄 Schema

```python
MEETING_NOTES_SCHEMA = {
    "executive_summary": "string",
    "attendees": ["string"],
    "agenda_items": [
        {
            "title": "string",
            "discussion": "string",
            "decisions": ["string"]
        }
    ],
    "action_items": [
        {
            "task": "string",
            "assignee": "string | null",
            "due_date": "string | null",
            "priority": "high | medium | low"
        }
    ],
    "key_decisions": ["string"],
    "follow_up_questions": ["string"]
}
```

```python
async def generate_meeting_notes(transcript: str) -> dict:
    prompt = f"""
你是一位專業的會議記錄助理。請根據以下逐字稿輸出結構化會議記錄。

重要規則：
1. assignee 和 due_date 只填寫逐字稿中明確提及的資訊
2. 若負責人未提及，assignee 填 null
3. 輸出必須是合法 JSON

逐字稿：
{transcript}
"""
    response = await call_llm(
        prompt,
        response_format={"type": "json_object"}
    )
    return json.loads(response)
```

## 匯入任務看板

```typescript
async function importActionItems(meetingId: string, actionItems: ActionItem[]) {
  const tasks = actionItems.map(item => ({
    title: item.task,
    assignee: item.assignee ?? "待指派",
    dueDate: item.due_date,
    priority: item.priority,
    sourceType: "meeting",
    sourceMeetingId: meetingId,
    status: "todo"
  }));

  await db.tasks.createMany({ data: tasks });
}
```

## 為什麼一定要結構化輸出？

<CardGroup cols={2}>
  <Card title="格式穩定，自動化才能運作" icon="table">
    固定 Schema 的 JSON 才能穩定匯入看板、觸發通知與更新狀態。
  </Card>

  <Card title="欄位明確，避免模型臆測" icon="shield-check">
    明確定義欄位可為 null，讓模型在資訊不足時留空，而不是自行捏造負責人或期限。
  </Card>

  <Card title="來源可追溯" icon="link">
    每個 Action Item 都連結到原始會議 ID，讓任何人都能回頭查看決策背景。
  </Card>

  <Card title="可搜尋、可查詢" icon="magnifying-glass">
    結構化資料支援跨會議精確查詢，例如找出所有指派給某人但尚未完成的任務。
  </Card>
</CardGroup>

## 踩坑筆記

<Warning>
  逐字稿沒有提到負責人或期限時，欄位應該留空，絕對不能由模型自行猜測。錯誤指派比空白更危險，因為它會讓人誤以為已有人負責。
</Warning>

<Accordion title="長音檔的記憶體與處理時間問題">
  60 分鐘以上的錄音，轉錄與摘要可能需要數分鐘。建議接收上傳後立即回傳任務 ID，由背景佇列處理，完成後再透過 WebSocket、輪詢或通知回報結果。
</Accordion>

<Accordion title="多語言與專業術語的轉錄挑戰">
  台灣企業會議常見中英夾雜與大量專案代號。可透過術語 Prompt、轉錄後規則校正或自訂模型改善辨識。
</Accordion>

<Info>
  今天只要記住一件事：AI 工作流的終點不是生成內容，而是讓內容進入下一個可追蹤的工作環節。
</Info>

下一篇，我們將整理企業 Agent 正式上線前的安全、測試與部署工作。